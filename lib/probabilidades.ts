// Mock de cálculo de probabilidades
export function calcularProbabilidade(jogo: string) {
  console.log(`[MOCK Probabilidades] Chamado para ${jogo}`);
  // retorna número entre 0 e 1 como exemplo
  return Math.random();
}
